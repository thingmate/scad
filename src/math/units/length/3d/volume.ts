/**
 * Calculates the volume of a rectangular prism given its dimensions.
 *
 * @return {number} The calculated volume of the rectangular prism.
 */
export function volume(x: number, y: number, z: number): number {
  return x * y * z;
}
