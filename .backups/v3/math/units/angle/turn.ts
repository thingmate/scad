/**
 * Converts the given angle from turns to turns (no-op).
 *
 * @param {number} angleInTurns - The angle in turns to be converted.
 * @return {number} The angle converted to turns.
 */
export function turn(angleInTurns: number): number {
  return angleInTurns;
}
